# LadiPage Backend — biến riêng app (nx serve)
# Biến shared libs (DB, JWT, Supabase, Stripe, LibreFang, …) → xem /.env ở root monorepo

PORT=7002

# Feature flag CRM V2 (chỉ ladipage-backend)
CRM_ENABLED=false