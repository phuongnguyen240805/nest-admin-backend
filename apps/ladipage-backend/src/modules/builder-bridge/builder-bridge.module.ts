import { Module } from '@nestjs/common';

/**
 * BuilderBridgeModule
 * Hỗ trợ server-side cho SDK Builder (nếu cần).
 * Có thể expose một số API internal cho visual builder.
 */
@Module({})
export class BuilderBridgeModule {}
