import type { LpFlow } from '@liora/ladipage-types';

export function mapFlowEditorRpcDetail(value: LpFlow): LpFlow {
  // TODO(PR-BC-09): align flow/show graph once FlowEntity graph JSONB lands.
  return value;
}
